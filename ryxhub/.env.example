# RyxHub UI Configuration

# vLLM API URL (default: http://localhost:8001)
# vLLM uses OpenAI-compatible API on this port
VITE_VLLM_API_URL=http://localhost:8001

# Ryx Backend URL for sessions, RAG, workflows (default: http://localhost:8420)
VITE_RYX_API_URL=http://localhost:8420

# Use Mock API (true = mock data, false = live vLLM backend)
# Default is true in development when vLLM is not running
VITE_USE_MOCK_API=true
